# Server-side keys only (read by `npm start` via --env-file). Never commit the real .env.
# Every key is optional: with none set the game is identical, with a written debrief and browser speech.
IFM_API_KEY=
GEMINI_API_KEY=
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
XAI_API_KEY=
